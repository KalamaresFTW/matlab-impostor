%Esta funcion calcula el area de un triangulo dadas la base y la altura
% Como llamar a la funcion: area(base, altura)
function y = area(base,height)
  y = base*height/2;  %b es la base y h es la altura
endfunction